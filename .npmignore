# Source files (compiled to dist/, which is in `files`)
src/
tests/
*.ts
!*.d.ts
tsconfig.json

# Project tooling
.github/
.claude/
.mcp.json
docs/
scripts/

# Project files (rendered into the GitHub release body, not the npm package)
CHANGELOG.md
CLAUDE.md

# Build/publish artifacts that shouldn't ship
*.tgz
node_modules/

# IDE / OS
.vscode/
.idea/
.DS_Store